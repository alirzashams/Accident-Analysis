# 🧠 Phase 4: Predictive Modeling (AI)
**مستندات فاز چهارم: پیش‌بینی شدت تصادفات با هوش مصنوعی**

## ۱. مقدمه (Introduction)
در این فاز، پروژه از سطح «تحلیلی» به سطح «پیش‌بینی» (Predictive) ارتقا یافت. هدف، ساخت مدلی است که بتواند بر اساس شرایط جاده و زمان، پیش‌بینی کند که **«اگر تصادفی رخ دهد، چقدر شدید خواهد بود؟»**

## ۲. انتخاب مدل (Model Selection)
از الگوریتم **XGBoost Classifier** استفاده شد.
* **دلیل انتخاب:** این الگوریتم مبتنی بر گرادیان تقویت‌شه (Gradient Boosting) است و در کار با داده‌های جدولی (Tabular Data) عملکردی بسیار بهتر از شبکه‌های عصبی ساده یا درخت تصمیم دارد. همچنین قابلیت مدیریت داده‌های نامتوازن را داراست.

## ۳. مهندسی ویژگی‌ها (Feature Engineering)
ویژگی‌های ورودی مدل شامل ترکیبی از داده‌های خام و ویژگی‌های تولید شده در فازهای قبل است:
* **ویژگی‌های زمانی:** ساعت وقوع (`Hour`)، روز هفته.
* **ویژگی‌های محیطی:** شرایط نور، آب‌وهوا، محدودیت سرعت.
* **ویژگی‌های مکانی پیشرفته:**
  * `Cluster`: شماره خوشه‌ای که در فاز ۲ توسط DBSCAN تولید شد.
  * `Density`: تراکم منطقه‌ای محاسبه شده توسط KDE.

## ۴. ارزیابی و نتایج (Evaluation)
* **اهمیت ویژگی‌ها (Feature Importance):** مدل نشان داد که متغیرهای **Cluster (مکان)** و **Speed Limit (سرعت)** تعیین‌کننده‌ترین عوامل در شدت تصادف هستند.
* **دقت مدل:** مدل توانست با دقت قابل قبولی الگوهای منجر به تصادفات "Fatal" را از تصادفات "Slight" تفکیک کند.