
# 🛠️ Phase 1: Data Preprocessing & Geo-Filtering
**مستندات فاز اول: پاکسازی داده‌ها و فیلتر جغرافیایی**

## ۱. مقدمه (Introduction)
داده‌های خام تصادفات جاده‌ای همواره حاوی نویز، مقادیر گم‌شده و خطاهای انسانی هستند. هدف این فاز، تبدیل داده‌های خام (`Raw Data`) به یک نسخه تمیز و قابل اعتماد (`Cleaned Data`) برای تحلیل‌های بعدی است.

## ۲. چالش‌های شناسایی شده
در بررسی اولیه دیتاست، مشکلات زیر شناسایی شد:
1. **مقادیر گم‌شده (Missing Values):** برخی ردیف‌ها فاقد مختصات جغرافیایی (`Latitude`, `Longitude`) بودند.
2. **داده‌های پرت مکانی (Spatial Outliers):** وجود نقاطی با مختصات نادرست که خارج از محدوده بریتانیا (مثلاً در اقیانوس یا کشورهای دیگر) ثبت شده بودند.
3. **فرمت زمان:** ستون‌های تاریخ و زمان به صورت رشته متنی (`String`) بودند که امکان پردازش زمانی را نمی‌دادند.

## ۳. اقدامات فنی (Technical Approach)

### ۳-۱. مدیریت داده‌های گم‌شده
از آنجا که هدف اصلی پروژه «تحلیل مکانی» است، ردیف‌هایی که فاقد مختصات جغرافیایی بودند حذف شدند. استفاده از روش‌های پر کردن (Imputation) مثل میانگین‌گیری برای مختصات جغرافیایی مناسب نیست زیرا باعث ایجاد خوشه‌های مصنوعی و گمراه‌کننده می‌شود.

### ۳-۲. فیلتر جغرافیایی (Geo-Filtering)
برای حذف داده‌های پرت، یک جعبه محدودکننده (Bounding Box) تقریبی برای بریتانیا تعریف شد:
* **Latitude:** بین ۴۹ تا ۶۱ درجه شمالی
* **Longitude:** بین ۱۰- تا ۲ درجه شرقی

```python
# منطق فیلتر در کد preprocess.py
uk_mask = (
    (df['Latitude'] > 49) & (df['Latitude'] < 61) &
    (df['Longitude'] > -10) & (df['Longitude'] < 2)
)
df_clean = df[uk_mask].copy()