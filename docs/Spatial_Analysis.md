
(مخصوص تحلیل مکانی با DBSCAN و KDE)

```markdown
# 🗺️ Phase 2: Spatial Analysis & Clustering
**مستندات فاز دوم: تحلیل مکانی و شناسایی کانون‌های خطر**

## ۱. هدف (Objective)
هدف این فاز، گذر از آمارهای توصیفی ساده و شناسایی الگوهای پنهان مکانی است. ما به دنبال پاسخ به این سوال هستیم: **«تصادفات دقیقاً در کجاها متمرکز شده‌اند؟»**

## ۲. متدولوژی (Methodology)

### ۲-۱. الگوریتم خوشه‌بندی DBSCAN
برای گروه‌بندی تصادفات، از الگوریتم **DBSCAN** استفاده شد.
* **چرا DBSCAN؟** برخلاف K-Means، این الگوریتم نیازی به دانستن تعداد خوشه‌ها از قبل ندارد و مهم‌تر از آن، قادر به تشخیص و حذف نقاط نویز (تصادفات پراکنده و تصادفی) است.
* **تنظیمات (Hyperparameters):**
  * `eps=0.01`: شعاع همسایگی تقریبی ۱ کیلومتر.
  * `min_samples=10`: حداقل ۱۰ تصادف برای تشکیل یک کانون خطر (Cluster).

### ۲-۲. تخمین چگالی هسته (KDE)
برای ایجاد یک نقشه پیوسته از خطر، از روش **Kernel Density Estimation (KDE)** با کرنل گوسی استفاده شد.
* **کاربرد:** این روش به هر نقطه از نقشه یک امتیاز «تراکم» اختصاص می‌دهد که نشان‌دهنده شدت وقوع تصادفات در آن ناحیه است. این امتیاز بعداً در فرمول شاخص خطر استفاده می‌شود.

## ۳. نتایج (Results)
* شناسایی موفقیت‌آمیز کانون‌های اصلی تصادفات (Hotspots).
* جداسازی نقاط نویز (Noise Points) که تحلیل آماری را منحرف می‌کردند.
* تولید ستون‌های جدید `Cluster` و `Density` در دیتاست.